# Use pnpm as the package manager for this project
# This prevents npm from creating package-lock.json and causing conflicts

# Enforce pnpm usage
engine-strict=true

# Disable package-lock.json creation
package-lock=false
